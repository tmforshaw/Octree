[package]
name = "chess_gui"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = {version = "0.16.*", features = ["dynamic_linking"]}
bevy_mod_picking = "0.20.*"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
